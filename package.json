{
  "name": "websocket-server",
  "version": "1.0.0",
  "description": "WebSocket server with encrypted JSON storage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.15.0"
  }
}
